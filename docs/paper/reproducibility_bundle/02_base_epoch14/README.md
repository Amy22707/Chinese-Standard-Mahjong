# Base-E14

Checkpoint index 14 from the shared base supervised run. It corresponds to validation epoch 15 (loss 0.3562, policy accuracy 0.8656) and is retained as an intermediate checkpoint and local opponent.

## 中文

这是同一条基础监督训练轨迹中的 checkpoint 14，对应第 15 次验证：验证损失为 0.3562，策略准确率为 0.8656。论文将它作为中期 checkpoint 和本地冻结对手；它不是一次独立训练。
